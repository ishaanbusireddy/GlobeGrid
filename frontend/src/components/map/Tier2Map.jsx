// Tier 2 — Standard flat 2D map (Section 11.1). Implemented in Phase 6;
// this placeholder keeps the tier switcher functional until then.
import React from 'react';

export default function Tier2Map() {
  return (
    <div className="tier-placeholder">
      Tier 2 (flat 2D map) is built in Phase 6 — switch to Tier 1 or Tier 3.
    </div>
  );
}
